export default {
    // Maximum line length
    printWidth: 80,
    // Number of spaces per indentation
    tabWidth: 2,
    // Use spaces instead of tabs
    useTabs: false,
    // Add semicolons at the end of statements
    semi: true,
    // Use single quotes for strings
    singleQuote: false,
    // Object property quotes: add only when needed
    quoteProps: "as-needed",
    // Use single quotes in JSX
    jsxSingleQuote: false,
    // Trailing commas: ES5 standard (objects, arrays, etc.)
    trailingComma: "none",
    // Add spaces inside object literals braces
    bracketSpacing: true,
    // JSX tag's > does not start on a new line
    bracketSameLine: false,
    // Don't add parentheses for single parameter arrow functions
    arrowParens: "avoid",
    // The formatting range covers the entire file
    rangeStart: 0,
    rangeEnd: Infinity,
    // Format without requiring @prettier annotation
    requirePragma: false,
    // Don't insert @format comment at the top of the file
    insertPragma: false,
    // Markdown text wrapping
    proseWrap: "preserve",
    // HTML whitespace sensitivity
    htmlWhitespaceSensitivity: "css",
    // Line ending: LF
    endOfLine: "lf",
    // Format embedded code
    embeddedLanguageFormatting: "auto"
};
