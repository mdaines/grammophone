{
  "private": true,
  "version": "0.0.0",
  "name": "grammophone",
  "devDependencies": {
    "browserify": "*",
    "ejsify": "*",
    "jest": "*",
    "jison": "^0.4.18",
    "jshint": "*"
  },
  "scripts": {
    "test": "jest",
    "generate-parser": "jison src/parser.y src/parser.l --module-type=commonjs --outfile=src/parser.js",
    "build": "browserify src/application.js --standalone ApplicationController --transform ejsify",
    "lint": "yarn jshint --exclude build,node_modules,lib,src/grammar/parser.js ."
  }
}
