{
  "private": true,
  "version": "0.0.0",
  "name": "grammophone",
  "dependencies": {
    "@lezer/lr": "*"
  },
  "devDependencies": {
    "browserify": "*",
    "jest": "*",
    "jshint": "*",
    "uglify-js": "*",
    "@lezer/generator": "*"
  },
  "dependencies": {
    "mithril": "*"
  },
  "scripts": {
    "test": "jest",
    "generate-lezer-parser": "yarn lezer-generator --cjs --output src/parser/rules.js src/parser/rules.grammar",
    "build": "browserify src/application.js --standalone ApplicationController",
    "lint": "yarn jshint --exclude build,node_modules,lib,src/grammar/parser.js ."
  }
}
