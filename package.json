{
  "scripts": {
    "test": "mocha \"test/**/*-test.js\"",
    "generate-parser": "jison src/grammar/parser.y src/grammar/parser.l --module-type=commonjs --outfile=src/grammar/parser.js",
    "bundle": "browserify src/grammar/index.js -o src/grammar.js --standalone Grammar --transform [ babelify --ignore parser.js --presets [ es2015 ] ]",
    "lint": "jshint --exclude ./node_modules,parser.js,bundle.js,calculations/lr.js src/grammar"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.22.0",
    "babelify": "^7.3.0",
    "browserify": "^14.1.0",
    "jison": "^0.4.17",
    "jshint": "^2.9.4",
    "mocha": "^3.2.0"
  }
}
