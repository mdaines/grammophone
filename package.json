{
  "private": true,
  "version": "0.0.0",
  "name": "grammophone",
  "devDependencies": {
    "jest": "*",
    "browserify": "*",
    "jison": "^0.4.18"
  },
  "scripts": {
    "test": "jest",
    "generate-parser": "jison src/parser.y src/parser.l --module-type=commonjs --outfile=src/parser.js",
    "build": "browserify src/index.js --standalone Grammar"
  }
}
