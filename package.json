{
  "private": true,
  "version": "0.0.0",
  "name": "grammophone",
  "devDependencies": {
    "browserify": "*",
    "ejsify": "*",
    "jest": "*",
    "jison": "^0.4.18"
  },
  "scripts": {
    "test": "jest",
    "generate-parser": "jison src/parser.y src/parser.l --module-type=commonjs --outfile=src/parser.js",
    "build": "browserify src/application.js --outfile assets/application.js --standalone ApplicationController --transform ejsify"
  }
}
