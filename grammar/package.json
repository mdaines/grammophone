{
  "scripts": {
    "test": "mocha \"test/**/*-test.js\"",
    "generate-parser": "jison parser.y parser.l --module-type=commonjs --outfile=parser.js",
    "bundle": "browserify index.js -o bundle.js --standalone Grammar --transform [ babelify --ignore parser.js --presets [ es2015 ] ]"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.22.0",
    "babelify": "^7.3.0",
    "browserify": "^14.1.0",
    "jison": "^0.4.17",
    "mocha": "^3.2.0"
  }
}
